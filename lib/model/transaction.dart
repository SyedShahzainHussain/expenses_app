class TransactionModel {
  String id, title;
  double amount;
  DateTime dateTime;

  TransactionModel({
    required this.id,
    required this.title,
    required this.amount,
    required this.dateTime,
  });
}
